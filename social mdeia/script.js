// Example JS for button actions

document.querySelectorAll(".btn").forEach(btn => {
    btn.addEventListener("click", () => {
        alert("Demo button clicked — attach real function later!");
    });
});